import { app } from './app';

document.addEventListener('DOMContentLoaded', () => {
    // this event is happening when HTML DOM is loaded
    // if we have some heavy content such as images and video, we don't have to wait for them



    // this is how we usually run our web code
    app();


});
